course_name=string[100];
course_count=0;

attempt_credit=0;
take_credit=0;
remain_credit=0;
total_credit_score = 0.0;

recommend=int[100];
take_course=int[100];

dependency=string[100];

input_buf=char[250];
old_i=0;
new_i=0;
tmp=0;
this_credit=0;

gets(input_buf);
while( input_buf[0]!=0 ){
    printf("%s",input_buf);
    endline();
    gets(input_buf);
}