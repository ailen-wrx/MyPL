course_name=string[100];
course_count=0;

attempt_credit=0;
take_credit=0;
remain_credit=0;
total_credit_score = 0.0;

recommend=int[100];
take_course=int[100];

dependency=string[100];

input_buf=char[250];

gets(input_buf);
while( input_buf[0]!=0 ){
	oldi = 0;
	a = char[40];
	memset(a, 0, 40);
	strncpy(a, input_buf + oldi, 5);

    printf("%s",a);
    endline();
    gets(input_buf);
}