course_name=string[100];
course_count=0;

attempt_credit=0;
take_credit=0;
remain_credit=0;
total_credit_score = 0.0;

recommend=int[100];
take_course=int[100];

dependency=string[100];

input_buf=char[250];

gets(input_buf);
while( input_buf[0]!=0 ){
	oldi = 0;
	course_name[course_count] = char[40];
	memset(course_name[course_count], 0, 40);
	strncpy(course_name[course_count], input_buf +0, 5);

    printf("%s",course_name[course_count]);
    endline();
    gets(input_buf);
}